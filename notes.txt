#ngrok.yml, at C:\Users\Admin\AppData\Local\ngrok\ngrok.yml

version: "2"
authtoken: "token_abc_number123"

tunnels:
  tunnel1:
    addr: 192.168.1.24:4000
    proto: http
  tunnel2:
    addr: 192.168.1.24:5000
    proto: http


#> must be in the ngrok.yml folder to start
start at cmd line with: "ngrok start --config ngrok.yml tunnel1 tunnel2"
# my example --> ngrok start --config ngrok.yml python nodejs



requirements:
pip freeze > requirements.txt

install:
pip install -r requirements.txt

run with python app.py  and npm start or node server.js